from django.db import models


'''class Create_Task(models.Model):
    title = models.CharField(max_length=50)
    estimate = models.DateField()


class Modify_Task(Create_Task):
    state = (
        (u'M', u'Male'),
        (u'F', u'Female'),
    )'''
